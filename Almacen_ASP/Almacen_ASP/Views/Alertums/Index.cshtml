@model IEnumerable<Almacen_ASP.Models.Alertum>

@{
    ViewData["Title"] = "ALERTAS";
}   

<h1>ALERTAS</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mensaje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProductoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mensaje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProductoNavigation.IdProducto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAlerta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAlerta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAlerta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
